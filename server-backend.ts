// =============================
// Backend code preserved for reference
// This file contains your full Express/Prisma backend, commented out for Vercel frontend deployment.
// To deploy backend, use a separate service (e.g., Render, Railway, Fly.io)
// =============================
/*
import dotenv from "dotenv";
import express, { Request, Response } from "express";
import bodyParser from "body-parser";
import cors from "cors";
import helmet from "helmet";
import rateLimit from "express-rate-limit";
import cookieParser from "cookie-parser";
import bcrypt from "bcryptjs";
import jwt, { JwtPayload } from "jsonwebtoken";
import { body, validationResult } from "express-validator";
import { PrismaClient, User } from "@prisma/client";
import { authenticateToken, AuthRequest } from "./middleware/auth.ts";

// ...rest of your backend code from server.ts...
*/
